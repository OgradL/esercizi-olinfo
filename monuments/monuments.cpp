#include <iostream>
#include <vector>
#include <unordered_map>
#include <algorithm>
using namespace std;

int r(int l, int r, vector<int>& pref){

}

int main(){

	int N, K, L;
	cin >> N >> K >> L;

	vector<int> A(N);
	for (int &n : A)
		cin >> n;
	
	sort(A.begin(), A.end());

	for (int i = 0; i < N; i++){
		
	}

	return 0;
}